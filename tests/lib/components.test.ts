// import { expect, test } from '@playwright/test';
// import PokemonType__SvelteComponent_ from 'lib/components/PokemonType.svelte';

// test.use({ viewport: { width: 500, height: 500 } });

// test('component stuff', async () => {
//     const componentRef = new PokemonType__SvelteComponent_({
//         target: new Element,
//         props: {
//             type: {
//                 name: 'bug',
//                 url: '',
//             }
//         }
//     });

//     const component = await mount(componentRef);

//     await expect(component)
// });